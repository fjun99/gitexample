# PR: Initialize Project Structure

**Status**: Ready for Review
**Related Task**: [Task-001](../tasks/task-001-init-project.md)

## Summary
Initialized the project using `uv`. This PR adds the basic Python project structure and configuration files.

## Changes
- Added `pyproject.toml` configuration.
- Added `main.py` entry point.
- Added `.gitignore` and `.python-version`.
- Added `docs/tasks/task-001-init-project.md` for task tracking.

## Testing
- [x] `uv run main.py` prints "Hello from gitsample-project!".
- [x] Verified `.venv` is ignored by `.gitignore`.

## Risks
None.
